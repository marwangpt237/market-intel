pyyaml>=6.0
# Phase 1: API
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.0.0
# Phase 2: Scheduler
apscheduler>=3.10.0
# Phase 5: PostgreSQL (optional — only needed for production)
# psycopg2-binary>=2.9.0
